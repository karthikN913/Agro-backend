rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow authenticated users full read/write access.
      // For production, replace with granular per-collection rules.
      allow read, write: if request.auth != null;
    }
  }
}
